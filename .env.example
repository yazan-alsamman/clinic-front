# Local dev: leave unset — Vite proxies /api to the backend (vite.config.ts).
# Production (static site on another host): full API origin, no trailing slash.
# VITE_API_BASE_URL=https://your-api.hostingersite.com
